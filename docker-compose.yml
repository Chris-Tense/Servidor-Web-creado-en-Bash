services:
  serverbash:
    image: christense/serverbash:latest
    container_name: serverbash
    ports:
      - "9999:8888"
    volumes:
      - serverbash_data:/opt/ServidorCalamar
    restart: always

volumes:
  serverbash_data:
